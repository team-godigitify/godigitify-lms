{
  "extends": "../../packages/typescript-config/nextjs.json",
  "compilerOptions": {
    // No baseUrl, no ignoreDeprecations needed
    "paths": {
      "@/*": ["./src/*"]  // already relative to tsconfig location, so no change needed here
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}